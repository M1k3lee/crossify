const TokenFactory = artifacts.require("TokenFactory");

module.exports = async function(deployer) {
  // Initial migration - just to initialize the deployment
  console.log("Initial migration completed");
};
